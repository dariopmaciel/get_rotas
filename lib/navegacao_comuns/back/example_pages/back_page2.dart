import 'package:flutter/material.dart';

class BackPage2 extends StatelessWidget {

  const BackPage2({ super.key });

   @override
   Widget build(BuildContext context) {
       return Scaffold(
           appBar: AppBar(title: const Text('Back Page 2'),),
           body: Container(),
       );
  }
}